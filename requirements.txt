Django>=6.1,<6.2      # needs Python 3.12+ and SQLite 3.37+
Pillow>=12.0          # ImageField support and local image downscaling
pypdf>=6.0            # reads the text layer out of digital PDFs
pypdfium2>=5.0        # renders scanned PDFs to images

# Only needed for the Claude backend; the free Gemini and Ollama backends
# talk plain HTTP and need nothing extra.
anthropic>=1.2,<2.0
